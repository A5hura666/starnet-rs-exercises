use std::sync::{Arc, Mutex};
use std::thread;
use crate::serveur::server_thread::{ServerThread};

mod serveur;
mod app_defines;
mod types;

fn main() -> Result<(), Box<dyn std::error::Error>> {
    // TODO : create vector of message

    // TODO : create server thread

    // TODO : start server thread
}
